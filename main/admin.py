from main.models import Kategoriya, Taom
from django.contrib import admin
from modeltranslation.admin import TranslationAdmin,admin

class TaomAdmin(TranslationAdmin):
    list_display = ('id', 'name', 'photo')
    class Media:
        js = (
            'http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js',
            'http://ajax.googleapis.com/ajax/libs/jqueryui/1.10.2/jquery-ui.min.js',
            'modeltranslation/js/tabbed_translation_fields.js',
        )
        css = {
            'screen': ('modeltranslation/css/tabbed_translation_fields.css',),
        }


admin.site.register(Taom, TaomAdmin)

class KategoriyaAdmin(TranslationAdmin):
    list_display = ('id', 'name','photo')

    class Media:
        js = (
            'http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js',
            'http://ajax.googleapis.com/ajax/libs/jqueryui/1.10.2/jquery-ui.min.js',
            'modeltranslation/js/tabbed_translation_fields.js',
        )
        css = {
            'screen': ('modeltranslation/css/tabbed_translation_fields.css',),
        }


admin.site.register(Kategoriya, KategoriyaAdmin)